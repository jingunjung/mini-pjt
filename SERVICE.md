# SERVICE · 국내 여행 플래너 Agent

## 1. 사용자·문제·가치
- 여행 계획 짜는 게 번거로운 직장인. 짧은 휴가를 알차게 쓰고 싶지만 일정 조사에 시간을
  쓰기 부담스럽다.
- 어떤 문제를 푸는가: 예산에 맞는 국내 여행 일정을 짜려면 블로그/유튜브를 일일이 뒤져야 하고,
  여행사 패키지는 비싸고 개인화가 안 된다.
- 이 에이전트 없을 때는: 블로그/유튜브 검색으로 정보를 일일이 수집하거나, 여행사 상품을
  이용하거나, 지인에게 문의한다. 이걸로 뭐가 좋아지는가: 총 예산과 취향(맛집중심/숨은여행지
  등)만 입력하면, 취향에 따라 가중치가 조정된 항목별(숙박/식비/교통/관광) 예산 배분과
  일자별 일정표를 몇 분 만에 받고, 모든 추천에는 실제 공공 관광 데이터 출처가 표기되어
  신뢰할 수 있다. 승인한 계획은 장기 기억에 저장되어 다음에 실행할 때 다시 꺼내볼 수 있다.
- 지원 범위: 여행 기간 1~7일, 인원 1~10명(혼자~가족 단위)까지 가변으로 받는다.

## 2. 서비스 확장 관점
- 현재는 개인 학습용 소규모 데모다. 향후 숙박/액티비티 예약 플랫폼과 연동해 예약 성사 시
  수수료를 받거나, 프리미엄 개인화 리포트를 구독형으로 제공하는 실서비스화 가설을 둔다.
- 규모: 1인 사용자가 여행 계획 시점(연 2~4회)마다 세션 단위로 사용. 여행사 대비 저비용·
  개인화, 블로그/유튜브 검색 대비 예산 준수·시간 절약이 대체제와의 차별점이다.

## 3. 사용 예상 도구·데이터
- 도구:
  1. `search_destination_info` — 관광지/맛집/숙소/숨은명소 RAG 검색 (MCP 서버로 노출)
  2. `estimate_round_trip_transport` — 출발지~목적지 왕복 교통비 추정기 (결정적 조회 테이블:
     서울↔부산/서울↔강릉 KTX, 제주행 국내선 항공만 지원. 모르는 노선은 지어내지 않고
     "정보 없음"으로 답한다)
  3. `calculate_budget_allocation` — 예산 계산/배분 계산기 (결정적 파이썬 함수, LLM 산술 금지).
     왕복 교통비를 알면 먼저 총 예산에서 제외하고, 남은 금액을 숙박/식비/현지교통/관광
     4개 항목으로 배분한다. 왕복 교통비를 모르면 현지 비용만 배분하고 그 사실을 안내한다.
  4. `validate_itinerary` — 일정표 시간 겹침·동선(지역 일관성) 검증 툴
- 데이터: 한국관광공사 TourAPI(`data/scripts/fetch_tourapi.py`)로 수집(또는 폴백으로 수동
  정리한) 지역별 관광지/맛집/숙소/숨은명소 정보를 정적 마크다운 문서화(`data/docs/`) → Chroma
  벡터DB(`data/chroma_travel/`)로 임베딩. 실제 공공데이터 기반의 오프라인 정적 스냅샷이며,
  범위는 국내 여행(제주/부산/강릉)으로 한정한다.

## 4. 서비스 정책 (가드레일 요약)
- 존재하지 않는 장소/정보를 날조하지 않는다 — RAG 문서에 근거 없으면 "찾을 수 없습니다"라고
  답한다.
- 예산 배분 합계는 입력한 총액을 절대 초과하지 않는다 — 계산은 전용 계산 툴이 전담하고,
  LLM이 직접 산술하지 않는다.
- 여행과 무관한 질문(잡담/업무 질문 등)에는 답변을 거부한다.
- 최종 일정 확정 전 반드시 사용자 승인(Human-in-the-loop) 단계를 거친다 (승인/수정/거절).
- 세부 케이스는 `evaluation/test_queries.csv`의 `guardrail`(프롬프트 인젝션·PII·범위 밖 요청
  차단)과 `negative`(환각·범위 밖 정보 거부) 카테고리에서 검증된다.

## 5. 성공 기준
- `evaluation/test_queries.csv` 20건(positive 8·negative 4·edge 5·guardrail 3) 중 80% 이상
  통과 (`evaluation/run_eval.py`가 `expected_traits` 충족 여부·`forbidden` 미포함 여부·
  `expected_tools` 호출 여부를 함께 판정)
- 예산 배분 결과가 입력 총액을 초과하는 케이스 0% (positive/edge 카테고리에서 100% 통과)
- 추천 장소의 환각(RAG 문서 미근거) 케이스 0% (negative 카테고리에서 100% 통과)
- "쓸만하다"의 기준: 국내 1~3박 여행 요청 시 별다른 수정 없이 바로 참고 가능한 일정표와
  예산표를 받을 수 있는 수준
